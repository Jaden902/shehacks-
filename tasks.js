// Populate initial tasks when the page loads
window.onload = () => {
    // Default category to show tasks for
    const defaultCategory = 'techExploration';
    showTasks(defaultCategory); // Show default tasks on load
};
